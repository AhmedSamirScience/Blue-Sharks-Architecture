<?xml version="1.0" encoding="utf-8" ?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
>
    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.bluearchitecture"
        tools:targetApi="31"
    >

        <!--
             🔥 This removes the default WorkManager initializer injected by AndroidX Startup.
             ⚠️ Why? Because we are using Configuration.Provider in MyApp to manually configure WorkManager
             with a HiltWorkerFactory. If we don't remove this, WorkManager may be initialized twice,
             causing runtime crashes or injection failure.
        -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="remove"
        />
        <!--
            android:name="androidx.startup.InitializationProvider"
            The class responsible for auto-initializing WorkManager -->

        <!--
            android:authorities="${applicationId}.androidx-startup"
            Matches the default authority string generated by WorkManager -->

        <!--
            android:exported="false"
            Required on Android 12+ to declare non-exported providers -->

        <!--
            tools:node="remove"
            Instructs the manifest merger to REMOVE this provider from the final APK -->

        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
